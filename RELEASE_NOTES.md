# v1.0.0 — Initial Windows and macOS Release

首次發布版本。

## Downloads

- Windows：`PDFUnlocker.exe`
- macOS：`PDFUnlocker-macOS.zip`

## Features

- 支援拖拉單一 PDF、多個 PDF，或整個資料夾。
- 支援批次處理與子資料夾。
- 支援中文檔名與中文路徑。
- 支援開啟密碼與權限限制 PDF。
- 原始檔不會被修改，輸出會放在 `unlocked` 資料夾。
- 提供 Windows `.exe` 與 macOS `.app` 建置流程。

## Important

本工具只適用於你有合法權限的 PDF。它不會破解未知密碼，也不提供暴力猜密碼功能。

macOS 首次開啟未簽章 app 時，可能需要在 Finder 右鍵點選「打開」，或到系統設定允許執行。
